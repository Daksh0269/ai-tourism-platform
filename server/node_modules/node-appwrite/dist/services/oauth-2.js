'use strict';

var client = require('../client');

class Oauth2 {
  constructor(client) {
    this.client = client;
  }
  approve(paramsOrFirst, ...rest) {
    let params;
    if (paramsOrFirst && typeof paramsOrFirst === "object" && !Array.isArray(paramsOrFirst)) {
      params = paramsOrFirst || {};
    } else {
      params = {
        grantId: paramsOrFirst,
        authorizationDetails: rest[0],
        scope: rest[1]
      };
    }
    const grantId = params.grantId;
    const authorizationDetails = params.authorizationDetails;
    const scope = params.scope;
    if (typeof grantId === "undefined") {
      throw new client.AppwriteException('Missing required parameter: "grantId"');
    }
    const apiPath = "/oauth2/{project_id}/approve".replace("{project_id}", encodeURIComponent(String(this.client.config.project)));
    const payload = {};
    if (typeof grantId !== "undefined") {
      payload["grant_id"] = grantId;
    }
    if (typeof authorizationDetails !== "undefined") {
      payload["authorization_details"] = authorizationDetails;
    }
    if (typeof scope !== "undefined") {
      payload["scope"] = scope;
    }
    const uri = new URL(this.client.config.endpoint + apiPath);
    const apiHeaders = {
      "content-type": "application/json",
      "accept": "application/json"
    };
    return this.client.call(
      "post",
      uri,
      apiHeaders,
      payload
    );
  }
  authorize(paramsOrFirst, ...rest) {
    let params;
    if (!paramsOrFirst || paramsOrFirst && typeof paramsOrFirst === "object" && !Array.isArray(paramsOrFirst)) {
      params = paramsOrFirst || {};
    } else {
      params = {
        clientId: paramsOrFirst,
        redirectUri: rest[0],
        responseType: rest[1],
        scope: rest[2],
        state: rest[3],
        nonce: rest[4],
        codeChallenge: rest[5],
        codeChallengeMethod: rest[6],
        prompt: rest[7],
        maxAge: rest[8],
        authorizationDetails: rest[9],
        resource: rest[10],
        audience: rest[11],
        requestUri: rest[12]
      };
    }
    const clientId = params.clientId;
    const redirectUri = params.redirectUri;
    const responseType = params.responseType;
    const scope = params.scope;
    const state = params.state;
    const nonce = params.nonce;
    const codeChallenge = params.codeChallenge;
    const codeChallengeMethod = params.codeChallengeMethod;
    const prompt = params.prompt;
    const maxAge = params.maxAge;
    const authorizationDetails = params.authorizationDetails;
    const resource = params.resource;
    const audience = params.audience;
    const requestUri = params.requestUri;
    const apiPath = "/oauth2/{project_id}/authorize".replace("{project_id}", encodeURIComponent(String(this.client.config.project)));
    const payload = {};
    if (typeof clientId !== "undefined") {
      payload["client_id"] = clientId;
    }
    if (typeof redirectUri !== "undefined") {
      payload["redirect_uri"] = redirectUri;
    }
    if (typeof responseType !== "undefined") {
      payload["response_type"] = responseType;
    }
    if (typeof scope !== "undefined") {
      payload["scope"] = scope;
    }
    if (typeof state !== "undefined") {
      payload["state"] = state;
    }
    if (typeof nonce !== "undefined") {
      payload["nonce"] = nonce;
    }
    if (typeof codeChallenge !== "undefined") {
      payload["code_challenge"] = codeChallenge;
    }
    if (typeof codeChallengeMethod !== "undefined") {
      payload["code_challenge_method"] = codeChallengeMethod;
    }
    if (typeof prompt !== "undefined") {
      payload["prompt"] = prompt;
    }
    if (typeof maxAge !== "undefined") {
      payload["max_age"] = maxAge;
    }
    if (typeof authorizationDetails !== "undefined") {
      payload["authorization_details"] = authorizationDetails;
    }
    if (typeof resource !== "undefined") {
      payload["resource"] = resource;
    }
    if (typeof audience !== "undefined") {
      payload["audience"] = audience;
    }
    if (typeof requestUri !== "undefined") {
      payload["request_uri"] = requestUri;
    }
    const uri = new URL(this.client.config.endpoint + apiPath);
    const apiHeaders = {
      "accept": "application/json"
    };
    return this.client.call(
      "get",
      uri,
      apiHeaders,
      payload
    );
  }
  authorizePost(paramsOrFirst, ...rest) {
    let params;
    if (!paramsOrFirst || paramsOrFirst && typeof paramsOrFirst === "object" && !Array.isArray(paramsOrFirst)) {
      params = paramsOrFirst || {};
    } else {
      params = {
        clientId: paramsOrFirst,
        redirectUri: rest[0],
        responseType: rest[1],
        scope: rest[2],
        state: rest[3],
        nonce: rest[4],
        codeChallenge: rest[5],
        codeChallengeMethod: rest[6],
        prompt: rest[7],
        maxAge: rest[8],
        authorizationDetails: rest[9],
        resource: rest[10],
        audience: rest[11],
        requestUri: rest[12]
      };
    }
    const clientId = params.clientId;
    const redirectUri = params.redirectUri;
    const responseType = params.responseType;
    const scope = params.scope;
    const state = params.state;
    const nonce = params.nonce;
    const codeChallenge = params.codeChallenge;
    const codeChallengeMethod = params.codeChallengeMethod;
    const prompt = params.prompt;
    const maxAge = params.maxAge;
    const authorizationDetails = params.authorizationDetails;
    const resource = params.resource;
    const audience = params.audience;
    const requestUri = params.requestUri;
    const apiPath = "/oauth2/{project_id}/authorize".replace("{project_id}", encodeURIComponent(String(this.client.config.project)));
    const payload = {};
    if (typeof clientId !== "undefined") {
      payload["client_id"] = clientId;
    }
    if (typeof redirectUri !== "undefined") {
      payload["redirect_uri"] = redirectUri;
    }
    if (typeof responseType !== "undefined") {
      payload["response_type"] = responseType;
    }
    if (typeof scope !== "undefined") {
      payload["scope"] = scope;
    }
    if (typeof state !== "undefined") {
      payload["state"] = state;
    }
    if (typeof nonce !== "undefined") {
      payload["nonce"] = nonce;
    }
    if (typeof codeChallenge !== "undefined") {
      payload["code_challenge"] = codeChallenge;
    }
    if (typeof codeChallengeMethod !== "undefined") {
      payload["code_challenge_method"] = codeChallengeMethod;
    }
    if (typeof prompt !== "undefined") {
      payload["prompt"] = prompt;
    }
    if (typeof maxAge !== "undefined") {
      payload["max_age"] = maxAge;
    }
    if (typeof authorizationDetails !== "undefined") {
      payload["authorization_details"] = authorizationDetails;
    }
    if (typeof resource !== "undefined") {
      payload["resource"] = resource;
    }
    if (typeof audience !== "undefined") {
      payload["audience"] = audience;
    }
    if (typeof requestUri !== "undefined") {
      payload["request_uri"] = requestUri;
    }
    const uri = new URL(this.client.config.endpoint + apiPath);
    const apiHeaders = {
      "content-type": "application/json",
      "accept": "application/json"
    };
    return this.client.call(
      "post",
      uri,
      apiHeaders,
      payload
    );
  }
  createDeviceAuthorization(paramsOrFirst, ...rest) {
    let params;
    if (!paramsOrFirst || paramsOrFirst && typeof paramsOrFirst === "object" && !Array.isArray(paramsOrFirst)) {
      params = paramsOrFirst || {};
    } else {
      params = {
        clientId: paramsOrFirst,
        scope: rest[0],
        authorizationDetails: rest[1],
        resource: rest[2],
        audience: rest[3]
      };
    }
    const clientId = params.clientId;
    const scope = params.scope;
    const authorizationDetails = params.authorizationDetails;
    const resource = params.resource;
    const audience = params.audience;
    const apiPath = "/oauth2/{project_id}/device_authorization".replace("{project_id}", encodeURIComponent(String(this.client.config.project)));
    const payload = {};
    if (typeof clientId !== "undefined") {
      payload["client_id"] = clientId;
    }
    if (typeof scope !== "undefined") {
      payload["scope"] = scope;
    }
    if (typeof authorizationDetails !== "undefined") {
      payload["authorization_details"] = authorizationDetails;
    }
    if (typeof resource !== "undefined") {
      payload["resource"] = resource;
    }
    if (typeof audience !== "undefined") {
      payload["audience"] = audience;
    }
    const uri = new URL(this.client.config.endpoint + apiPath);
    const apiHeaders = {
      "content-type": "application/json",
      "accept": "application/json"
    };
    return this.client.call(
      "post",
      uri,
      apiHeaders,
      payload
    );
  }
  createGrant(paramsOrFirst) {
    let params;
    if (paramsOrFirst && typeof paramsOrFirst === "object" && !Array.isArray(paramsOrFirst)) {
      params = paramsOrFirst || {};
    } else {
      params = {
        userCode: paramsOrFirst
      };
    }
    const userCode = params.userCode;
    if (typeof userCode === "undefined") {
      throw new client.AppwriteException('Missing required parameter: "userCode"');
    }
    const apiPath = "/oauth2/{project_id}/grants".replace("{project_id}", encodeURIComponent(String(this.client.config.project)));
    const payload = {};
    if (typeof userCode !== "undefined") {
      payload["user_code"] = userCode;
    }
    const uri = new URL(this.client.config.endpoint + apiPath);
    const apiHeaders = {
      "content-type": "application/json",
      "accept": "application/json"
    };
    return this.client.call(
      "post",
      uri,
      apiHeaders,
      payload
    );
  }
  getGrant(paramsOrFirst) {
    let params;
    if (paramsOrFirst && typeof paramsOrFirst === "object" && !Array.isArray(paramsOrFirst)) {
      params = paramsOrFirst || {};
    } else {
      params = {
        grantId: paramsOrFirst
      };
    }
    const grantId = params.grantId;
    if (typeof grantId === "undefined") {
      throw new client.AppwriteException('Missing required parameter: "grantId"');
    }
    const apiPath = "/oauth2/{project_id}/grants/{grant_id}".replace("{project_id}", encodeURIComponent(String(this.client.config.project))).replace("{grant_id}", encodeURIComponent(String(grantId)));
    const payload = {};
    const uri = new URL(this.client.config.endpoint + apiPath);
    const apiHeaders = {
      "accept": "application/json"
    };
    return this.client.call(
      "get",
      uri,
      apiHeaders,
      payload
    );
  }
  listOrganizations(paramsOrFirst, ...rest) {
    let params;
    if (!paramsOrFirst || paramsOrFirst && typeof paramsOrFirst === "object" && !Array.isArray(paramsOrFirst)) {
      params = paramsOrFirst || {};
    } else {
      params = {
        limit: paramsOrFirst,
        offset: rest[0],
        search: rest[1]
      };
    }
    const limit = params.limit;
    const offset = params.offset;
    const search = params.search;
    const apiPath = "/oauth2/{project_id}/organizations".replace("{project_id}", encodeURIComponent(String(this.client.config.project)));
    const payload = {};
    if (typeof limit !== "undefined") {
      payload["limit"] = limit;
    }
    if (typeof offset !== "undefined") {
      payload["offset"] = offset;
    }
    if (typeof search !== "undefined") {
      payload["search"] = search;
    }
    const uri = new URL(this.client.config.endpoint + apiPath);
    const apiHeaders = {
      "accept": "application/json"
    };
    return this.client.call(
      "get",
      uri,
      apiHeaders,
      payload
    );
  }
  createPAR(paramsOrFirst, ...rest) {
    let params;
    if (paramsOrFirst && typeof paramsOrFirst === "object" && !Array.isArray(paramsOrFirst)) {
      params = paramsOrFirst || {};
    } else {
      params = {
        clientId: paramsOrFirst,
        redirectUri: rest[0],
        responseType: rest[1],
        scope: rest[2],
        state: rest[3],
        nonce: rest[4],
        codeChallenge: rest[5],
        codeChallengeMethod: rest[6],
        prompt: rest[7],
        maxAge: rest[8],
        authorizationDetails: rest[9],
        resource: rest[10],
        audience: rest[11]
      };
    }
    const clientId = params.clientId;
    const redirectUri = params.redirectUri;
    const responseType = params.responseType;
    const scope = params.scope;
    const state = params.state;
    const nonce = params.nonce;
    const codeChallenge = params.codeChallenge;
    const codeChallengeMethod = params.codeChallengeMethod;
    const prompt = params.prompt;
    const maxAge = params.maxAge;
    const authorizationDetails = params.authorizationDetails;
    const resource = params.resource;
    const audience = params.audience;
    if (typeof clientId === "undefined") {
      throw new client.AppwriteException('Missing required parameter: "clientId"');
    }
    if (typeof redirectUri === "undefined") {
      throw new client.AppwriteException('Missing required parameter: "redirectUri"');
    }
    if (typeof responseType === "undefined") {
      throw new client.AppwriteException('Missing required parameter: "responseType"');
    }
    const apiPath = "/oauth2/{project_id}/par".replace("{project_id}", encodeURIComponent(String(this.client.config.project)));
    const payload = {};
    if (typeof clientId !== "undefined") {
      payload["client_id"] = clientId;
    }
    if (typeof redirectUri !== "undefined") {
      payload["redirect_uri"] = redirectUri;
    }
    if (typeof responseType !== "undefined") {
      payload["response_type"] = responseType;
    }
    if (typeof scope !== "undefined") {
      payload["scope"] = scope;
    }
    if (typeof state !== "undefined") {
      payload["state"] = state;
    }
    if (typeof nonce !== "undefined") {
      payload["nonce"] = nonce;
    }
    if (typeof codeChallenge !== "undefined") {
      payload["code_challenge"] = codeChallenge;
    }
    if (typeof codeChallengeMethod !== "undefined") {
      payload["code_challenge_method"] = codeChallengeMethod;
    }
    if (typeof prompt !== "undefined") {
      payload["prompt"] = prompt;
    }
    if (typeof maxAge !== "undefined") {
      payload["max_age"] = maxAge;
    }
    if (typeof authorizationDetails !== "undefined") {
      payload["authorization_details"] = authorizationDetails;
    }
    if (typeof resource !== "undefined") {
      payload["resource"] = resource;
    }
    if (typeof audience !== "undefined") {
      payload["audience"] = audience;
    }
    const uri = new URL(this.client.config.endpoint + apiPath);
    const apiHeaders = {
      "content-type": "application/json",
      "accept": "application/json"
    };
    return this.client.call(
      "post",
      uri,
      apiHeaders,
      payload
    );
  }
  listProjects(paramsOrFirst, ...rest) {
    let params;
    if (!paramsOrFirst || paramsOrFirst && typeof paramsOrFirst === "object" && !Array.isArray(paramsOrFirst)) {
      params = paramsOrFirst || {};
    } else {
      params = {
        limit: paramsOrFirst,
        offset: rest[0],
        search: rest[1]
      };
    }
    const limit = params.limit;
    const offset = params.offset;
    const search = params.search;
    const apiPath = "/oauth2/{project_id}/projects".replace("{project_id}", encodeURIComponent(String(this.client.config.project)));
    const payload = {};
    if (typeof limit !== "undefined") {
      payload["limit"] = limit;
    }
    if (typeof offset !== "undefined") {
      payload["offset"] = offset;
    }
    if (typeof search !== "undefined") {
      payload["search"] = search;
    }
    const uri = new URL(this.client.config.endpoint + apiPath);
    const apiHeaders = {
      "accept": "application/json"
    };
    return this.client.call(
      "get",
      uri,
      apiHeaders,
      payload
    );
  }
  reject(paramsOrFirst) {
    let params;
    if (paramsOrFirst && typeof paramsOrFirst === "object" && !Array.isArray(paramsOrFirst)) {
      params = paramsOrFirst || {};
    } else {
      params = {
        grantId: paramsOrFirst
      };
    }
    const grantId = params.grantId;
    if (typeof grantId === "undefined") {
      throw new client.AppwriteException('Missing required parameter: "grantId"');
    }
    const apiPath = "/oauth2/{project_id}/reject".replace("{project_id}", encodeURIComponent(String(this.client.config.project)));
    const payload = {};
    if (typeof grantId !== "undefined") {
      payload["grant_id"] = grantId;
    }
    const uri = new URL(this.client.config.endpoint + apiPath);
    const apiHeaders = {
      "content-type": "application/json",
      "accept": "application/json"
    };
    return this.client.call(
      "post",
      uri,
      apiHeaders,
      payload
    );
  }
  revoke(paramsOrFirst, ...rest) {
    let params;
    if (paramsOrFirst && typeof paramsOrFirst === "object" && !Array.isArray(paramsOrFirst)) {
      params = paramsOrFirst || {};
    } else {
      params = {
        token: paramsOrFirst,
        tokenTypeHint: rest[0],
        clientId: rest[1],
        clientSecret: rest[2]
      };
    }
    const token = params.token;
    const tokenTypeHint = params.tokenTypeHint;
    const clientId = params.clientId;
    const clientSecret = params.clientSecret;
    if (typeof token === "undefined") {
      throw new client.AppwriteException('Missing required parameter: "token"');
    }
    const apiPath = "/oauth2/{project_id}/revoke".replace("{project_id}", encodeURIComponent(String(this.client.config.project)));
    const payload = {};
    if (typeof token !== "undefined") {
      payload["token"] = token;
    }
    if (typeof tokenTypeHint !== "undefined") {
      payload["token_type_hint"] = tokenTypeHint;
    }
    if (typeof clientId !== "undefined") {
      payload["client_id"] = clientId;
    }
    if (typeof clientSecret !== "undefined") {
      payload["client_secret"] = clientSecret;
    }
    const uri = new URL(this.client.config.endpoint + apiPath);
    const apiHeaders = {
      "content-type": "application/json",
      "accept": "application/json"
    };
    return this.client.call(
      "post",
      uri,
      apiHeaders,
      payload
    );
  }
  createToken(paramsOrFirst, ...rest) {
    let params;
    if (paramsOrFirst && typeof paramsOrFirst === "object" && !Array.isArray(paramsOrFirst)) {
      params = paramsOrFirst || {};
    } else {
      params = {
        grantType: paramsOrFirst,
        code: rest[0],
        refreshToken: rest[1],
        deviceCode: rest[2],
        clientId: rest[3],
        clientSecret: rest[4],
        codeVerifier: rest[5],
        redirectUri: rest[6],
        resource: rest[7],
        audience: rest[8]
      };
    }
    const grantType = params.grantType;
    const code = params.code;
    const refreshToken = params.refreshToken;
    const deviceCode = params.deviceCode;
    const clientId = params.clientId;
    const clientSecret = params.clientSecret;
    const codeVerifier = params.codeVerifier;
    const redirectUri = params.redirectUri;
    const resource = params.resource;
    const audience = params.audience;
    if (typeof grantType === "undefined") {
      throw new client.AppwriteException('Missing required parameter: "grantType"');
    }
    const apiPath = "/oauth2/{project_id}/token".replace("{project_id}", encodeURIComponent(String(this.client.config.project)));
    const payload = {};
    if (typeof grantType !== "undefined") {
      payload["grant_type"] = grantType;
    }
    if (typeof code !== "undefined") {
      payload["code"] = code;
    }
    if (typeof refreshToken !== "undefined") {
      payload["refresh_token"] = refreshToken;
    }
    if (typeof deviceCode !== "undefined") {
      payload["device_code"] = deviceCode;
    }
    if (typeof clientId !== "undefined") {
      payload["client_id"] = clientId;
    }
    if (typeof clientSecret !== "undefined") {
      payload["client_secret"] = clientSecret;
    }
    if (typeof codeVerifier !== "undefined") {
      payload["code_verifier"] = codeVerifier;
    }
    if (typeof redirectUri !== "undefined") {
      payload["redirect_uri"] = redirectUri;
    }
    if (typeof resource !== "undefined") {
      payload["resource"] = resource;
    }
    if (typeof audience !== "undefined") {
      payload["audience"] = audience;
    }
    const uri = new URL(this.client.config.endpoint + apiPath);
    const apiHeaders = {
      "content-type": "application/json",
      "accept": "application/json"
    };
    return this.client.call(
      "post",
      uri,
      apiHeaders,
      payload
    );
  }
}

exports.Oauth2 = Oauth2;
//# sourceMappingURL=oauth-2.js.map
//# sourceMappingURL=oauth-2.js.map